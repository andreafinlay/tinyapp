<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body bgcolor="#FCF3FF">
  <header>
    <% include partials/_header %>
  </header>
  <button><a href="/urls/new">Create A New Short Link</a></button>
  <h3><font face="Courier">Your Shortened Links:</font></h3>
  <ul>
    <% for (var objKey in privateURLs) { %>
      <li><font face="Courier"><%= objKey %> : <%= privateURLs[objKey] %></font></li>
        <form method="POST" action="/urls/<%= objKey %>/delete">
          <button> Delete Link </button>
        </form>
        <button><a href="/urls/<%= objKey %>"> Edit Link </a></button>
    <% } %>
  </ul>
</body>
</html>
